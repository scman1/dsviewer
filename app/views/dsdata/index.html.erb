<main role="main">
  <%= render 'shared/jumbotron' %>
  <div class="album py-5 bg-light">
<table  class="table table-striped table-bordered table-responsive">
  <thead>
    <tr>
      <th> Specimen</th>
      <th class="d-none d-lg-block"> MIDS Level</th>
      <th>Scientific Name</th>
      <th>Country</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @dsdata.each do |dsdatum| %>
      <tr>
        <td><%= dsdatum.ds_id %></td>
        <td class="d-none d-lg-block"><%= dsdatum.mids_level %></td>
        <td><%= dsdatum.scientific_name %></td>
        <td><%= dsdatum.country %></td>
        <td>
            <% @id=dsdatum.ds_id.to_s() %>
            <%= link_to 'Show', {:controller => 'dsdata',
              :action => 'show', :id => @id}, :class => "btn btn-primary btn-xs" %> 
        </td>
        <td><%= link_to 'Edit', {:controller => 'dsdata',
              :action => 'edit', :id => @id}, :class => "btn btn-success btn-xs" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div> <!--album div-->

